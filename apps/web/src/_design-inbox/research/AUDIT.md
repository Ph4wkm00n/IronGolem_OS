# Audit — research

> Generated by `scripts/design-component-audit.ts` on 2026-05-12.

## Summary

- **18** blockers — must address before promotion
- **2** should-fix items — recommended swaps
- **6** info items — review and graduate if you see a pattern

**Files scanned (1):**

- `research/project/Research.tsx`

## 🛑 BLOCKER · Dynamic Tailwind tone interpolation (15)

Template-string class names with `${tone}` are invisible to Tailwind's JIT compiler. The resulting class won't ship in the CSS bundle and the element will render unstyled. Lift the tone lookup into a static `TONE` classmap (see `pages/v2/Home.tsx` for the canonical pattern).

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 542 | `bg-${tone}` |
| `research/project/Research.tsx` | 542 | `text-${tone}` |
| `research/project/Research.tsx` | 542 | `border-${tone}` |
| `research/project/Research.tsx` | 544 | `bg-${tone}` |
| `research/project/Research.tsx` | 559 | `bg-${tone}` |
| `research/project/Research.tsx` | 572 | `bg-${m.tone}` |
| `research/project/Research.tsx` | 572 | `text-${m.tone}` |
| `research/project/Research.tsx` | 572 | `border-${m.tone}` |
| `research/project/Research.tsx` | 602 | `bg-${tone}` |
| `research/project/Research.tsx` | 719 | `bg-${agreementTone}` |
| `research/project/Research.tsx` | 719 | `text-${agreementTone}` |
| `research/project/Research.tsx` | 728 | `bg-${agreementTone}` |
| `research/project/Research.tsx` | 728 | `text-${agreementTone}` |
| `research/project/Research.tsx` | 728 | `border-${agreementTone}` |
| `research/project/Research.tsx` | 761 | `text-${tone}` |

**Recommendation:**

- Add a `TONE` classmap (see `apps/web/src/pages/v2/Home.tsx` lines ~80-110) and reference `TONE[name].bg` / `.text` / `.border` instead.

## 🛑 BLOCKER · Browser-preview shim (1)

Bottom-of-file `(window as ...).<Route> = <Route>` exists only so Claude Design's HTML preview can mount the route. Production builds should drop this entirely.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 1217 | `;(window as unknown as { Research: typeof Research }).Research = Research;` |

**Recommendation:**

- Delete this line in the production port. The shim exists only so the design's preview HTML can mount the route without a bundler.

## 🛑 BLOCKER · `JSX.Element` return-type annotation (1)

TypeScript 6 deprecates the global `JSX` namespace. Remove the annotation entirely — TS infers the right return type from the JSX.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 1074 | `export function Research(): JSX.Element {` |

**Recommendation:**

- Remove the `: JSX.Element` annotation — TS infers the right return type from the function body. (TS 6+ retires the global JSX namespace.)

## 🛑 BLOCKER · `bg-app` Tailwind class (1)

The design's `bg-app` CSS variable isn't wired in this project. Use `bg-neutral-50` instead.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 1146 | `<main className="min-h-screen bg-app">` |

**Recommendation:**

- Replace `bg-app` with `bg-neutral-50`. The design's `--bg-app` CSS variable isn't wired in this project — `bg-neutral-50` is the production app-shell surface.

## ⚠️  SHOULD-FIX · Local reimplementation of an @irongolem/ui component (1)

This design file defines a function with the same name as a component already exported from `@irongolem/ui`. Replace the local definition with `import { X } from "@irongolem/ui"` unless the local divergence is intentional.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 609 | `function ResearchCard({` |

**Recommendation:**

- Replace with `import { ResearchCard } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.

## ⚠️  SHOULD-FIX · Namespace React import (1)

Project style is `import React, { useState } from "react"`. The namespace form makes named hooks awkward and is inconsistent with the rest of v2.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 20 | `import * as React from "react";` |

**Recommendation:**

- Switch to `import React, { useState, useMemo, useEffect } from "react"` and update any `React.useState` / `React.useMemo` calls in the file to use the named hooks directly.

## ℹ️  INFO · Possible @irongolem/ui graduation candidate (6)

Top-level component functions that don't match an existing @irongolem/ui export. Most are page-local helpers and stay inline; flag for review only if you see the same shape appear in a second route — that's the point to graduate.

| File | Line | Excerpt |
|---|---:|---|
| `research/project/Research.tsx` | 550 | `function ConfidenceBar({ value, size = "sm" }: { value: number; size?: "sm" \| "md" }) {` |
| `research/project/Research.tsx` | 598 | `function ImpactDot({ impact }: { impact: Impact }) {` |
| `research/project/Research.tsx` | 897 | `function SortPicker({ value, onChange }: { value: SortKey; onChange: (v: SortKey) => void }) {` |
| `research/project/Research.tsx` | 924 | `function TopicChips({ value, counts, onChange }: {` |
| `research/project/Research.tsx` | 986 | `function EmptyState() {` |
| `research/project/Research.tsx` | 1041 | `function FilterBar({` |

**Recommendation:**

- Inline component `ConfidenceBar`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/ConfidenceBar.tsx`.
- Inline component `ImpactDot`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/ImpactDot.tsx`.
- Inline component `SortPicker`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/SortPicker.tsx`.
- Inline component `TopicChips`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/TopicChips.tsx`.
- Inline component `EmptyState`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/EmptyState.tsx`.
- Inline component `FilterBar`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/FilterBar.tsx`.

---

## How to use this report

1. Read top-down — blockers first, then should-fix, then info.
2. Accept or reject each finding consciously. Some `should-fix` items may be intentional (e.g. a route's local `SafetyCard` could be intentionally divergent from the @irongolem/ui shape; the audit can't know).
3. Apply the accepted changes when translating to `apps/web/src/pages/v2/<Route>.tsx`.
4. Capture an Interceptor visual baseline once the production port is live.

This file is regenerated every run — feel free to delete it once the route is ported.
