# Audit — recipes

> Generated by `scripts/design-component-audit.ts` on 2026-05-12.

## Summary

- **26** blockers — must address before promotion
- **4** should-fix items — recommended swaps
- **9** info items — review and graduate if you see a pattern

**Files scanned (1):**

- `recipes/project/Recipes.tsx`

## 🛑 BLOCKER · Dynamic Tailwind tone interpolation (23)

Template-string class names with `${tone}` are invisible to Tailwind's JIT compiler. The resulting class won't ship in the CSS bundle and the element will render unstyled. Lift the tone lookup into a static `TONE` classmap (see `pages/v2/Home.tsx` for the canonical pattern).

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 836 | `bg-${m}` |
| `recipes/project/Recipes.tsx` | 836 | `text-${m}` |
| `recipes/project/Recipes.tsx` | 836 | `border-${m}` |
| `recipes/project/Recipes.tsx` | 838 | `bg-${m}` |
| `recipes/project/Recipes.tsx` | 849 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 849 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 852 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 875 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 875 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 875 | `border-${m.tone}` |
| `recipes/project/Recipes.tsx` | 892 | `text-${tone}` |
| `recipes/project/Recipes.tsx` | 893 | `text-${tone}` |
| `recipes/project/Recipes.tsx` | 899 | `bg-${tone}` |
| `recipes/project/Recipes.tsx` | 1055 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1055 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1060 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1195 | `text-${trustTone}` |
| `recipes/project/Recipes.tsx` | 1197 | `text-${trustTone}` |
| `recipes/project/Recipes.tsx` | 1291 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1291 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1300 | `bg-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1300 | `text-${m.tone}` |
| `recipes/project/Recipes.tsx` | 1300 | `border-${m.tone}` |

**Recommendation:**

- Add a `TONE` classmap (see `apps/web/src/pages/v2/Home.tsx` lines ~80-110) and reference `TONE[name].bg` / `.text` / `.border` instead.

## 🛑 BLOCKER · Browser-preview shim (1)

Bottom-of-file `(window as ...).<Route> = <Route>` exists only so Claude Design's HTML preview can mount the route. Production builds should drop this entirely.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 1696 | `;(window as unknown as { Recipes: typeof Recipes }).Recipes = Recipes;` |

**Recommendation:**

- Delete this line in the production port. The shim exists only so the design's preview HTML can mount the route without a bundler.

## 🛑 BLOCKER · `JSX.Element` return-type annotation (1)

TypeScript 6 deprecates the global `JSX` namespace. Remove the annotation entirely — TS infers the right return type from the JSX.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 1587 | `export function Recipes(): JSX.Element {` |

**Recommendation:**

- Remove the `: JSX.Element` annotation — TS infers the right return type from the function body. (TS 6+ retires the global JSX namespace.)

## 🛑 BLOCKER · `bg-app` Tailwind class (1)

The design's `bg-app` CSS variable isn't wired in this project. Use `bg-neutral-50` instead.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 1628 | `<main className="min-h-screen bg-app">` |

**Recommendation:**

- Replace `bg-app` with `bg-neutral-50`. The design's `--bg-app` CSS variable isn't wired in this project — `bg-neutral-50` is the production app-shell surface.

## ⚠️  SHOULD-FIX · Local reimplementation of an @irongolem/ui component (3)

This design file defines a function with the same name as a component already exported from `@irongolem/ui`. Replace the local definition with `import { X } from "@irongolem/ui"` unless the local divergence is intentional.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 829 | `function RiskBadge({ level, size = "sm" }: { level: Risk; size?: "sm" \| "md" }) {` |
| `recipes/project/Recipes.tsx` | 884 | `function SafetyCard({ safety }: { safety: SafetyShape }) {` |
| `recipes/project/Recipes.tsx` | 981 | `function PolicyCard({ layers }: { layers: PolicyLayer[] }) {` |

**Recommendation:**

- Replace with `import { RiskBadge } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.
- Replace with `import { SafetyCard } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.
- Replace with `import { PolicyCard } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.

## ⚠️  SHOULD-FIX · Namespace React import (1)

Project style is `import React, { useState } from "react"`. The namespace form makes named hooks awkward and is inconsistent with the rest of v2.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 18 | `import * as React from "react";` |

**Recommendation:**

- Switch to `import React, { useState, useMemo, useEffect } from "react"` and update any `React.useState` / `React.useMemo` calls in the file to use the named hooks directly.

## ℹ️  INFO · Possible @irongolem/ui graduation candidate (9)

Top-level component functions that don't match an existing @irongolem/ui export. Most are page-local helpers and stay inline; flag for review only if you see the same shape appear in a second route — that's the point to graduate.

| File | Line | Excerpt |
|---|---:|---|
| `recipes/project/Recipes.tsx` | 844 | `function StatusBadge({ status }: { status: RecipeStatus }) {` |
| `recipes/project/Recipes.tsx` | 870 | `function PermissionBadge({ p }: { p: Permission }) {` |
| `recipes/project/Recipes.tsx` | 925 | `function SafetyCardPreview({` |
| `recipes/project/Recipes.tsx` | 1034 | `function RunsTimeline({ runs }: { runs: RunEvent[] }) {` |
| `recipes/project/Recipes.tsx` | 1102 | `function CategoryTabs({` |
| `recipes/project/Recipes.tsx` | 1146 | `function PrimaryAction({` |
| `recipes/project/Recipes.tsx` | 1177 | `function RunOnceButton({ onClick }: { onClick: () => void }) {` |
| `recipes/project/Recipes.tsx` | 1187 | `function TrustStrip({ recipe }: { recipe: Recipe }) {` |
| `recipes/project/Recipes.tsx` | 1496 | `function EmptyState({ tab, q }: { tab: TabId; q: string }) {` |

**Recommendation:**

- Inline component `StatusBadge`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/StatusBadge.tsx`.
- Inline component `PermissionBadge`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/PermissionBadge.tsx`.
- Inline component `SafetyCardPreview`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/SafetyCardPreview.tsx`.
- Inline component `RunsTimeline`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/RunsTimeline.tsx`.
- Inline component `CategoryTabs`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/CategoryTabs.tsx`.
- Inline component `PrimaryAction`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/PrimaryAction.tsx`.
- Inline component `RunOnceButton`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/RunOnceButton.tsx`.
- Inline component `TrustStrip`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/TrustStrip.tsx`.
- Inline component `EmptyState`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/EmptyState.tsx`.

---

## How to use this report

1. Read top-down — blockers first, then should-fix, then info.
2. Accept or reject each finding consciously. Some `should-fix` items may be intentional (e.g. a route's local `SafetyCard` could be intentionally divergent from the @irongolem/ui shape; the audit can't know).
3. Apply the accepted changes when translating to `apps/web/src/pages/v2/<Route>.tsx`.
4. Capture an Interceptor visual baseline once the production port is live.

This file is regenerated every run — feel free to delete it once the route is ported.
