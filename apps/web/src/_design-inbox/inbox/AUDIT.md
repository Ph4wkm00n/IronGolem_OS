# Audit — inbox

> Generated by `scripts/design-component-audit.ts` on 2026-05-12.

## Summary

- **14** blockers — must address before promotion
- **4** should-fix items — recommended swaps
- **7** info items — review and graduate if you see a pattern

**Files scanned (1):**

- `inbox/project/Inbox.tsx`

## 🛑 BLOCKER · Dynamic Tailwind tone interpolation (13)

Template-string class names with `${tone}` are invisible to Tailwind's JIT compiler. The resulting class won't ship in the CSS bundle and the element will render unstyled. Lift the tone lookup into a static `TONE` classmap (see `pages/v2/Home.tsx` for the canonical pattern).

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 760 | `bg-${m}` |
| `inbox/project/Inbox.tsx` | 760 | `text-${m}` |
| `inbox/project/Inbox.tsx` | 760 | `border-${m}` |
| `inbox/project/Inbox.tsx` | 762 | `bg-${m}` |
| `inbox/project/Inbox.tsx` | 775 | `text-${tone}` |
| `inbox/project/Inbox.tsx` | 776 | `text-${tone}` |
| `inbox/project/Inbox.tsx` | 782 | `bg-${tone}` |
| `inbox/project/Inbox.tsx` | 826 | `bg-${tone}` |
| `inbox/project/Inbox.tsx` | 849 | `bg-${c.tone}` |
| `inbox/project/Inbox.tsx` | 849 | `border-${c.tone}` |
| `inbox/project/Inbox.tsx` | 1239 | `border-${tone}` |
| `inbox/project/Inbox.tsx` | 1241 | `text-${tone}` |
| `inbox/project/Inbox.tsx` | 1245 | `text-${tone}` |

**Recommendation:**

- Add a `TONE` classmap (see `apps/web/src/pages/v2/Home.tsx` lines ~80-110) and reference `TONE[name].bg` / `.text` / `.border` instead.

## 🛑 BLOCKER · Browser-preview shim (1)

Bottom-of-file `(window as ...).<Route> = <Route>` exists only so Claude Design's HTML preview can mount the route. Production builds should drop this entirely.

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 1525 | `(window as any).Inbox = Inbox;` |

**Recommendation:**

- Delete this line in the production port. The shim exists only so the design's preview HTML can mount the route without a bundler.

## ⚠️  SHOULD-FIX · Local reimplementation of an @irongolem/ui component (2)

This design file defines a function with the same name as a component already exported from `@irongolem/ui`. Replace the local definition with `import { X } from "@irongolem/ui"` unless the local divergence is intentional.

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 753 | `function RiskBadge({ level, size = "sm" }: { level: Risk; size?: "sm" \| "md" }) {` |
| `inbox/project/Inbox.tsx` | 769 | `function SafetyCard({ safety }: { safety: SafetyShape }) {` |

**Recommendation:**

- Replace with `import { RiskBadge } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.
- Replace with `import { SafetyCard } from "@irongolem/ui"` unless this local version intentionally diverges. The integrator should reconcile prop shapes before deleting the local def.

## ⚠️  SHOULD-FIX · Namespace React import (1)

Project style is `import React, { useState } from "react"`. The namespace form makes named hooks awkward and is inconsistent with the rest of v2.

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 12 | `import * as React from "react";` |

**Recommendation:**

- Switch to `import React, { useState, useMemo, useEffect } from "react"` and update any `React.useState` / `React.useMemo` calls in the file to use the named hooks directly.

## ⚠️  SHOULD-FIX · Inline `<style>` keyframes (1)

`globals.css` already ships `ig-pulse`, `ig-toast-in`, `ig-slide-in`. Reuse the existing keyframes rather than re-declaring them inline.

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 1249 | `@keyframes ig` |

**Recommendation:**

- If this keyframe matches one already in `apps/web/src/styles/globals.css` (`ig-pulse`, `ig-toast-in`, `ig-slide-in`), drop the inline `<style>` block and apply the existing class. Otherwise consider promoting the keyframe to globals.css.

## ℹ️  INFO · Possible @irongolem/ui graduation candidate (7)

Top-level component functions that don't match an existing @irongolem/ui export. Most are page-local helpers and stay inline; flag for review only if you see the same shape appear in a second route — that's the point to graduate.

| File | Line | Excerpt |
|---|---:|---|
| `inbox/project/Inbox.tsx` | 824 | `function StatusDot({ status }: { status: Status }) {` |
| `inbox/project/Inbox.tsx` | 829 | `function FilterChips({` |
| `inbox/project/Inbox.tsx` | 926 | `function DraftedBlock({ draft, editing, onChange }: {` |
| `inbox/project/Inbox.tsx` | 1041 | `function OriginChips({ item }: { item: Item }) {` |
| `inbox/project/Inbox.tsx` | 1063 | `function WhyCallout({ cause }: { cause: string }) {` |
| `inbox/project/Inbox.tsx` | 1231 | `function Toast({ toast }: { toast: { kind: "approved" \| "denied" \| "snoozed"; title: string } }) {` |
| `inbox/project/Inbox.tsx` | 1254 | `function EmptyInbox() {` |

**Recommendation:**

- Inline component `StatusDot`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/StatusDot.tsx`.
- Inline component `FilterChips`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/FilterChips.tsx`.
- Inline component `DraftedBlock`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/DraftedBlock.tsx`.
- Inline component `OriginChips`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/OriginChips.tsx`.
- Inline component `WhyCallout`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/WhyCallout.tsx`.
- Inline component `Toast`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/Toast.tsx`.
- Inline component `EmptyInbox`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/EmptyInbox.tsx`.

---

## How to use this report

1. Read top-down — blockers first, then should-fix, then info.
2. Accept or reject each finding consciously. Some `should-fix` items may be intentional (e.g. a route's local `SafetyCard` could be intentionally divergent from the @irongolem/ui shape; the audit can't know).
3. Apply the accepted changes when translating to `apps/web/src/pages/v2/<Route>.tsx`.
4. Capture an Interceptor visual baseline once the production port is live.

This file is regenerated every run — feel free to delete it once the route is ported.
