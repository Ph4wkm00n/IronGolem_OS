# Audit — dashboard

> Generated by `scripts/design-component-audit.ts` on 2026-05-12.

## Summary

- **20** blockers — must address before promotion
- **2** should-fix items — recommended swaps
- **7** info items — review and graduate if you see a pattern

**Files scanned (1):**

- `dashboard/project/workspace-dashboard.jsx`

## 🛑 BLOCKER · Dynamic Tailwind tone interpolation (19)

Template-string class names with `${tone}` are invisible to Tailwind's JIT compiler. The resulting class won't ship in the CSS bundle and the element will render unstyled. Lift the tone lookup into a static `TONE` classmap (see `pages/v2/Home.tsx` for the canonical pattern).

| File | Line | Excerpt |
|---|---:|---|
| `dashboard/project/workspace-dashboard.jsx` | 345 | `text-${tone}` |
| `dashboard/project/workspace-dashboard.jsx` | 349 | `text-${tone}` |
| `dashboard/project/workspace-dashboard.jsx` | 479 | `bg-${scopeTone}` |
| `dashboard/project/workspace-dashboard.jsx` | 479 | `text-${scopeTone}` |
| `dashboard/project/workspace-dashboard.jsx` | 508 | `bg-${team.color}` |
| `dashboard/project/workspace-dashboard.jsx` | 508 | `text-${team.color}` |
| `dashboard/project/workspace-dashboard.jsx` | 519 | `bg-${scoreTone}` |
| `dashboard/project/workspace-dashboard.jsx` | 519 | `text-${scoreTone}` |
| `dashboard/project/workspace-dashboard.jsx` | 519 | `border-${scoreTone}` |
| `dashboard/project/workspace-dashboard.jsx` | 531 | `bg-${tone}` |
| `dashboard/project/workspace-dashboard.jsx` | 559 | `text-${meta.color}` |
| `dashboard/project/workspace-dashboard.jsx` | 642 | `bg-${meta.color}` |
| `dashboard/project/workspace-dashboard.jsx` | 648 | `bg-${team.color}` |
| `dashboard/project/workspace-dashboard.jsx` | 658 | `bg-${m}` |
| `dashboard/project/workspace-dashboard.jsx` | 658 | `text-${m}` |
| `dashboard/project/workspace-dashboard.jsx` | 658 | `border-${m}` |
| `dashboard/project/workspace-dashboard.jsx` | 660 | `bg-${m}` |
| `dashboard/project/workspace-dashboard.jsx` | 732 | `border-${tone}` |
| `dashboard/project/workspace-dashboard.jsx` | 736 | `text-${tone}` |

**Recommendation:**

- Add a `TONE` classmap (see `apps/web/src/pages/v2/Home.tsx` lines ~80-110) and reference `TONE[name].bg` / `.text` / `.border` instead.

## 🛑 BLOCKER · Browser-preview shim (1)

Bottom-of-file `(window as ...).<Route> = <Route>` exists only so Claude Design's HTML preview can mount the route. Production builds should drop this entirely.

| File | Line | Excerpt |
|---|---:|---|
| `dashboard/project/workspace-dashboard.jsx` | 745 | `window.WorkspaceDashboard = WorkspaceDashboard;` |

**Recommendation:**

- Delete this line in the production port. The shim exists only so the design's preview HTML can mount the route without a bundler.

## ⚠️  SHOULD-FIX · Inline `<style>` keyframes (2)

`globals.css` already ships `ig-pulse`, `ig-toast-in`, `ig-slide-in`. Reuse the existing keyframes rather than re-declaring them inline.

| File | Line | Excerpt |
|---|---:|---|
| `dashboard/project/workspace-dashboard.jsx` | 635 | `@keyframes slide` |
| `dashboard/project/workspace-dashboard.jsx` | 740 | `@keyframes toast` |

**Recommendation:**

- If this keyframe matches one already in `apps/web/src/styles/globals.css` (`ig-pulse`, `ig-toast-in`, `ig-slide-in`), drop the inline `<style>` block and apply the existing class. Otherwise consider promoting the keyframe to globals.css.

## ℹ️  INFO · Possible @irongolem/ui graduation candidate (7)

Top-level component functions that don't match an existing @irongolem/ui export. Most are page-local helpers and stay inline; flag for review only if you see the same shape appear in a second route — that's the point to graduate.

| File | Line | Excerpt |
|---|---:|---|
| `dashboard/project/workspace-dashboard.jsx` | 358 | `function FilterChips({ active, onChange, counts }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 391 | `function TrustBanner({ counts, safety, onOpenPolicy }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 434 | `function PermissionsRail({ events, teams }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 640 | `function StatusMarkSmall({ status }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 644 | `function TeamPillSmall({ team }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 653 | `function RiskBadgeSmall({ risk }) {` |
| `dashboard/project/workspace-dashboard.jsx` | 723 | `function Toast({ toast }) {` |

**Recommendation:**

- Inline component `FilterChips`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/FilterChips.tsx`.
- Inline component `TrustBanner`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/TrustBanner.tsx`.
- Inline component `PermissionsRail`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/PermissionsRail.tsx`.
- Inline component `StatusMarkSmall`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/StatusMarkSmall.tsx`.
- Inline component `TeamPillSmall`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/TeamPillSmall.tsx`.
- Inline component `RiskBadgeSmall`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/RiskBadgeSmall.tsx`.
- Inline component `Toast`. If the same shape appears in a second route, consider graduating it to `packages/ui/src/components/Toast.tsx`.

---

## How to use this report

1. Read top-down — blockers first, then should-fix, then info.
2. Accept or reject each finding consciously. Some `should-fix` items may be intentional (e.g. a route's local `SafetyCard` could be intentionally divergent from the @irongolem/ui shape; the audit can't know).
3. Apply the accepted changes when translating to `apps/web/src/pages/v2/<Route>.tsx`.
4. Capture an Interceptor visual baseline once the production port is live.

This file is regenerated every run — feel free to delete it once the route is ported.
